events {}

http {
    # WebSocket/HTTP upgrade support
    map $http_upgrade $connection_upgrade {
        default close;
        upgrade upgrade;
    }

    # Common proxy settings
    proxy_http_version 1.1;
    proxy_read_timeout 600s;
    proxy_connect_timeout 5s;
    proxy_redirect off;
    client_max_body_size 50m;

    upstream k8s_http {
        server host.docker.internal:32080;
    }

    # HTTP on 20080 (preferred)
    server {
        listen 20080;
        server_name _;
        location / {
            proxy_set_header Host api.local;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Request-Start "t=$msec";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_pass http://k8s_http;
        }
    }

    # HTTP on 21080 (preferred)
    server {
        listen 21080;
        server_name _;

        location / {
            # Set a fixed Host header for Gateway HTTPRoute host matching
            proxy_set_header Host app.local;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Request-Start "t=$msec";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;

            proxy_pass http://k8s_http;
        }
    }

    # consul-ui
    server {
        listen 8500;
        server_name _;

        location / {
            # Set a fixed Host header for Gateway HTTPRoute host matching
            proxy_set_header Host consul.local;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Request-Start "t=$msec";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;

            proxy_pass http://k8s_http;
        }
    }

    # stocat-gw
    server {
        listen 8080;
        server_name _;

        location / {
            # Set a fixed Host header for Gateway HTTPRoute host matching
            proxy_set_header Host stocat-gw.local;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Request-Start "t=$msec";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;

            proxy_pass http://k8s_http;
        }
    }

    # kiali
    server {
        listen 20001;
        server_name _;

        location / {
            # Set a fixed Host header for Gateway HTTPRoute host matching
            proxy_set_header Host kiali.local;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Request-Start "t=$msec";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;

            proxy_pass http://k8s_http;
        }
    }

    # hyperdx
    server {
        listen 3000;
        server_name _;

        location / {
            # Set a fixed Host header for Gateway HTTPRoute host matching
            proxy_set_header Host hyperdx.local;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Request-Start "t=$msec";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;

            proxy_pass http://k8s_http;
        }
    }

    # hyperdx
    server {
        listen 8123;
        server_name _;

        location / {
            # Set a fixed Host header for Gateway HTTPRoute host matching
            proxy_set_header Host clickhouse.local;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Port $server_port;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Request-Start "t=$msec";
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;

            proxy_pass http://k8s_http;
        }
    }
    
}
